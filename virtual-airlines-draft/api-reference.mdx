---
title: Virtual Airlines API Reference
description: Connect an airline website, dashboard, or private tool to the read-only Virtual Airlines API.
---

# Virtual Airlines API Reference

This page is for airline owners, managers, and people building an airline website, dashboard, roster page, or private tool.

The Virtual Airlines API lets an approved integration read selected information about one airline. It is deliberately read-only: it can show an airline profile, pilots, flights, fleet, schedules, and aggregate statistics, but it cannot book flights, change ticket prices, move aircraft, manage people, edit routes, or spend FH$.

The API is designed to be understandable and safe to integrate. Every API key belongs to exactly one airline, every key has explicit read-only scopes, list results are paginated, and internal database IDs are not exposed.

Mintlify also generates an interactive reference page for each endpoint from the [OpenAPI specification](https://docs.flyhub.app/virtual-airlines-draft/openapi.json). The generated pages are intentionally hidden from navigation but remain available through these direct links. Use the playground only with a disposable or appropriately scoped key; never paste a manager session token into it.

## Start here

1. The airline owner opens **Manage airline → Ownership → Integrations**.
2. Create an API key with a descriptive name, the smallest set of scopes the integration needs, and an optional expiry.
3. Copy the secret immediately. FlyHub displays it only once.
4. Store it on your website or service server. Never put it in browser JavaScript, a public repository, an embed, or a Discord message.
5. Call the public endpoint with the key in the `Authorization` header.

The public API base is:

```text
https://flyhub.app/api/v1/va
```

The resource endpoint is:

```text
GET https://flyhub.app/api/v1/va/{resource}
```

Example:

```bash
curl --fail-with-body \
  -H "Authorization: Bearer ${FHVA_API_KEY}" \
  "https://flyhub.app/api/v1/va/airline"
```

The `${FHVA_API_KEY}` variable should exist only in the server environment of your integration. A browser-facing page should call your server, and your server should call FlyHub.

## Two different API layers

There are two API layers in the product. Keeping them separate prevents most integration mistakes.

| Layer | Who uses it | What it is for |
| --- | --- | --- |
| `/api/v1/va/{resource}` | Airline websites, dashboards, and private integrations | Stable, read-only airline data using a FlyHub VA API key. |
| `/api/va/...` | FlyHub's own web and desktop screens | First-party actions such as booking, finance, fleet management, people management, and Ownership controls. |

Only the versioned `/api/v1/va/...` routes are the external read API. The `/api/va/...` routes require a FlyHub session and are not a supported public integration contract. Do not copy them into a third-party app and do not put a FlyHub session token in a public website.

## Authentication

Send the key as a bearer credential:

```http
Authorization: Bearer fhva_live_<public-part>_<secret-part>
```

Keys generated by FlyHub have this shape:

```text
fhva_live_XXXXXXXXXXX_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

The exact generated key contains an 11-character public part and a 43-character secret part. The example above is only a shape, not a usable key.

FlyHub stores a hash of the complete key, not the raw secret. The secret is returned only in the successful response that creates the key. If it is lost, create a replacement key and revoke the old one.

The airline is derived from the key. There is no `airlineId` query parameter, and a caller cannot use a key issued to one airline to read another airline.

### Authentication outcomes

| Status | Meaning | What to do |
| --- | --- | --- |
| `401` | The header is missing or the key is invalid, revoked, or expired. | Check the server secret, then create a replacement key if necessary. |
| `403` | The key is valid but does not include the scope required by this resource. | Add the scope to a new key, or use a key that already has it. |
| `404` | The resource name is not part of the public API. | Check the path and use one of the six resources listed below. |
| `429` | A request or row budget was exceeded. | Honor `Retry-After` and the reset headers; reduce polling and page size. |
| `503` | FlyHub cannot safely serve the request at the moment. | Retry with backoff. Do not disable your integration's security checks to work around it. |

The airline attached to a key must also be in an operating state accepted by the public API. Permanently closed or otherwise unavailable airlines cannot be read through an active integration key.

## Scopes

Give a key only the scopes it needs. Scopes cannot write to the airline.

| Scope | Resource | Includes |
| --- | --- | --- |
| `airline:read` | `airline` | Name, codes, home base, status, business model, application mode, summary, and counts. |
| `pilots:read` | `pilots` | Active roster names, ranks, membership dates, activity, flights, hours, contribution points, and reliability. |
| `flights:read` | `flights` | Public logbook activity, routes, aircraft, times, duration, distance, score, and result status. |
| `fleet:read` | `fleet` | Active aircraft registrations, types, stations, condition, maintenance state, hours, cycles, and availability. |
| `schedules:read` | `schedules` | Published network schedules, routes, days, planned block time, eligible aircraft, and schedule status. |
| `statistics:read` | `statistics` | Aggregate airline, pilot, fleet, schedule, contribution, and reliability totals. |

The available scope names are returned by the Ownership API when an owner lists API keys. A key may contain one or more scopes, up to all six.

## Common request parameters

The list resources accept the same two query parameters:

| Parameter | Default | Limits | Meaning |
| --- | ---: | ---: | --- |
| `limit` | `25` | `1`–`100` | Number of records requested. |
| `offset` | `0` | `0`–`10000` | Number of records skipped before the page. |

Example:

```text
GET /api/v1/va/flights?limit=50&offset=100
```

Invalid or missing numeric parameters fall back to their defaults instead of returning a validation error. A `limit` of `0` is treated as `1`. Values above the maximum are capped. There is no cursor endpoint at the moment, so an integration that needs a large list should page carefully and respect the offset ceiling.

The `limit` and `offset` parameters do not change the shape of `airline` or `statistics`, which are single objects.

## Response envelopes

### A single object

`airline` and `statistics` return:

```json
{
  "data": {},
  "meta": {
    "apiVersion": "2026-07-21"
  }
}
```

### A paginated list

`pilots`, `flights`, `fleet`, and `schedules` return:

```json
{
  "data": [],
  "pagination": {
    "offset": 0,
    "limit": 25,
    "total": 0
  },
  "meta": {
    "apiVersion": "2026-07-21"
  }
}
```

`total` is the total number of records available for that airline under the resource's filters, not the number returned on the current page.

Dates and timestamps are ISO 8601 values in UTC. A value can be `null` when FlyHub has no value to report. Treat unknown fields as forward-compatible: ignore fields you do not recognize rather than failing the whole response.

### Error envelope

Public API errors use this shape:

```json
{
  "error": {
    "code": "rate_limited",
    "message": "VA API rate or hourly row budget exceeded"
  }
}
```

The supported public error codes are `unauthorized`, `forbidden`, `not_found`, `rate_limited`, and `service_unavailable`.

## Public API endpoints

| Method | Endpoint | Scope | Result |
| --- | --- | --- | --- |
| `GET` | `/api/v1/va/airline` | `airline:read` | One airline profile object. |
| `GET` | `/api/v1/va/pilots` | `pilots:read` | Active roster, paginated. |
| `GET` | `/api/v1/va/flights` | `flights:read` | Airline logbook activity, paginated. |
| `GET` | `/api/v1/va/fleet` | `fleet:read` | Non-retired aircraft, paginated. |
| `GET` | `/api/v1/va/schedules` | `schedules:read` | Active and suspended schedules, paginated. |
| `GET` | `/api/v1/va/statistics` | `statistics:read` | Aggregate statistics object. |

There are no public `POST`, `PATCH`, or `DELETE` endpoints under `/api/v1/va`. A `POST` to a public resource is not a way to book a flight or update an airline.

### Generated endpoint pages

| Endpoint | Interactive reference |
| --- | --- |
| `GET /api/v1/va/airline` | [Airline profile](https://docs.flyhub.app/virtual-airlines-draft/api/airline) |
| `GET /api/v1/va/pilots` | [Pilots](https://docs.flyhub.app/virtual-airlines-draft/api/pilots) |
| `GET /api/v1/va/flights` | [Flights](https://docs.flyhub.app/virtual-airlines-draft/api/flights) |
| `GET /api/v1/va/fleet` | [Fleet](https://docs.flyhub.app/virtual-airlines-draft/api/fleet) |
| `GET /api/v1/va/schedules` | [Schedules](https://docs.flyhub.app/virtual-airlines-draft/api/schedules) |
| `GET /api/v1/va/statistics` | [Statistics](https://docs.flyhub.app/virtual-airlines-draft/api/statistics) |
| Ownership integration routes | [Integration management references](https://docs.flyhub.app/virtual-airlines-draft/api/management/integrations) |

## `GET /api/v1/va/airline`

Returns the identity and high-level status of the airline attached to the key. It is not paginated.

```bash
curl \
  -H "Authorization: Bearer ${FHVA_API_KEY}" \
  "https://flyhub.app/api/v1/va/airline"
```

Example response:

```json
{
  "data": {
    "name": "Example Air",
    "shortName": "Example",
    "slug": "example-air",
    "summary": "A scheduled international virtual airline.",
    "icaoCode": "EXA",
    "callsign": "EXAMPLE",
    "homeIcao": "SKBO",
    "homeCountryCode": "CO",
    "status": "active",
    "businessModel": "scheduled",
    "applicationMode": "open",
    "pilotCount": 18,
    "fleetCount": 7,
    "createdAt": "2026-07-21T00:00:00.000Z",
    "updatedAt": "2026-07-21T12:00:00.000Z"
  },
  "meta": {
    "apiVersion": "2026-07-21"
  }
}
```

`pilotCount` counts active airline memberships. `fleetCount` excludes aircraft whose operational status is `retired`.

## `GET /api/v1/va/pilots`

Returns the airline's active roster. Suspended, rejected, left, and removed memberships are not returned.

```bash
curl \
  -H "Authorization: Bearer ${FHVA_API_KEY}" \
  "https://flyhub.app/api/v1/va/pilots?limit=25&offset=0"
```

Each item contains:

| Field | Meaning |
| --- | --- |
| `displayName` | The pilot name shown by FlyHub. |
| `rank` | Current rank in this airline. |
| `joinedAt` | Time the active membership began. |
| `lastActivityAt` | Most recent tracked airline activity, when available. |
| `completedFlights` | Completed-flight total in the airline pilot statistics. |
| `completedHours` | Completed hours, rounded to one decimal place. |
| `contributionPoints` | Contribution points attributed to the pilot in this airline. |
| `reliabilityScore` | Reliability value used by the airline statistics, defaulting to `100` when no value exists. |
| `lastFlightAt` | Most recent completed flight time, or `null`. |

The list is ordered by `joinedAt` from oldest to newest. User IDs are intentionally omitted. Use a display name plus the airline context for presentation, not as a guaranteed permanent database identifier.

## `GET /api/v1/va/flights`

Returns the airline's public logbook activity, newest first. The result is a record of airline flight entries, not a command to start or book a flight. Always inspect `status`: a row can describe a successful, cancelled, rejected, or otherwise closed result.

```bash
curl \
  -H "Authorization: Bearer ${FHVA_API_KEY}" \
  "https://flyhub.app/api/v1/va/flights?limit=25&offset=0"
```

Each item contains:

| Field | Meaning |
| --- | --- |
| `status` | Current logbook entry status. Treat this as the result authority. |
| `flightNumber` | Airline flight number. |
| `originIcao` | Origin airport ICAO code. |
| `destinationIcao` | Destination airport ICAO code. |
| `aircraftRegistration` | Aircraft registration used for the entry. |
| `aircraftTypeIcao` | Aircraft type code. |
| `offBlockAt` | Off-block timestamp, when available. |
| `onBlockAt` | On-block timestamp, when available. |
| `blockMinutes` | Recorded block time in minutes. |
| `distanceNm` | Recorded distance in nautical miles. |
| `score` | Flight score, or `null` when no score was recorded. |
| `virtualPeriod` | Virtual operating period attached to the entry, when available. |

The public record does not expose internal booking, result, pilot, or aircraft UUIDs.

## `GET /api/v1/va/fleet`

Returns active and non-retired aircraft associated with the airline, ordered by registration.

```bash
curl \
  -H "Authorization: Bearer ${FHVA_API_KEY}" \
  "https://flyhub.app/api/v1/va/fleet?limit=100"
```

Each item contains:

| Field | Meaning |
| --- | --- |
| `registration` | Aircraft registration. |
| `displayName` | Airline name for the aircraft, when set. |
| `aircraftTypeIcao` | Aircraft type code. |
| `ownershipType` | Ownership category, such as a leased or owned asset. |
| `operationalStatus` | Current operational state. |
| `currentIcao` | Last recorded airport location, when available. |
| `conditionPercent` | Current aircraft condition percentage. |
| `maintenanceState` | Current maintenance state. |
| `airframeHours` | Airframe time, rounded to one decimal place. |
| `airframeCycles` | Airframe cycle count. |
| `availableFrom` | Time at which the aircraft can become available, when applicable. |
| `maintenanceDueAt` | Maintenance due timestamp, when applicable. |
| `acquiredAt` | Time the airline acquired the aircraft, when available. |
| `updatedAt` | Last update time for the public record. |

Retired aircraft are excluded. The public API does not provide a fleet-management action.

## `GET /api/v1/va/schedules`

Returns schedules whose template status is `active` or `suspended`, ordered by flight number. Archived or other non-public schedule states are not included.

```bash
curl \
  -H "Authorization: Bearer ${FHVA_API_KEY}" \
  "https://flyhub.app/api/v1/va/schedules?limit=100&offset=0"
```

Each item contains:

| Field | Meaning |
| --- | --- |
| `flightNumber` | Published airline flight number. |
| `originIcao` | Route origin. |
| `destinationIcao` | Route destination. |
| `operationType` | Route operation type, such as passenger or cargo. |
| `operatingDaysMask` | Seven-day schedule mask. |
| `plannedBlockMinutes` | Planned block time in minutes. |
| `eligibleAircraftTypes` | Aircraft type codes accepted by the schedule. |
| `status` | `active` or `suspended`. |
| `version` | Schedule version number. |
| `validFrom` | Schedule validity start, when set. |
| `validUntil` | Schedule validity end, when set. |
| `updatedAt` | Last update time. |

### Reading `operatingDaysMask`

The mask uses Monday-first bits:

| Bit value | Day |
| ---: | --- |
| `1` | Monday |
| `2` | Tuesday |
| `4` | Wednesday |
| `8` | Thursday |
| `16` | Friday |
| `32` | Saturday |
| `64` | Sunday |

For example, `31` (`1 + 2 + 4 + 8 + 16`) means Monday through Friday. Operating-day checks in Virtual Airlines use UTC, so convert a timestamp to UTC before deciding whether a schedule operates.

## `GET /api/v1/va/statistics`

Returns one aggregate object. It is deliberately summary-only and has no pagination.

```bash
curl \
  -H "Authorization: Bearer ${FHVA_API_KEY}" \
  "https://flyhub.app/api/v1/va/statistics"
```

Current fields:

| Field | Meaning |
| --- | --- |
| `completedFlights` | Count of airline logbook entries included by the current aggregate. |
| `completedHours` | Sum of airline pilot completed minutes expressed in hours and rounded to one decimal. |
| `activePilots` | Active membership count. |
| `fleetSize` | Non-retired aircraft count. |
| `activeSchedules` | Active schedule-template count. |
| `contributionPoints` | Sum of airline pilot contribution points. |
| `averageReliabilityScore` | Average pilot reliability rounded to two decimals, or `null` if no statistic exists. |

Statistics are aggregate values, not a replacement for the paginated resource records. For example, use `pilots` when you need the roster and `fleet` when you need individual aircraft.

## Rate limits and row budgets

The public API applies more than one protection. The response exposes the relevant values in headers:

| Header | Meaning |
| --- | --- |
| `Cache-Control: private, no-store` | The response is private and should not be stored as a shared cache entry. |
| `X-RateLimit-Limit` | Requests allowed for the current public API key/IP window. |
| `X-RateLimit-Remaining` | Requests left in that window. |
| `X-RateLimit-Reset` | ISO timestamp when the request window resets. |
| `X-RateLimit-Row-Limit` | Hourly row budget. |
| `X-RateLimit-Row-Remaining` | Rows left in the hourly budget. |
| `X-RateLimit-Row-Reset` | ISO timestamp when the row budget resets. |
| `Retry-After` | Seconds to wait on a `429` response. |

Current limits are:

- 120 authentication attempts per IP per minute before key lookup;
- 60 authenticated requests per API-key/IP pair per minute;
- 2,000 row-cost units per API key per hour;
- a `statistics` request costs 25 row units;
- an `airline` request costs 1 row unit;
- a list request costs the requested page size, with a minimum cost of 1.

If Redis-backed rate limiting is unavailable, FlyHub returns `503` instead of silently falling back to a weaker per-process limit. Keep your integration's retry policy bounded and honor the reset headers.

### A polite polling pattern

For a dashboard, fetch only the resources it actually displays, use the smallest useful `limit`, and refresh on a human scale rather than every few seconds. On `429`, pause until `Retry-After`; on `503`, retry with exponential backoff. Do not run several identical requests in parallel.

## A small server-side client

The following example keeps the key on a server and handles the common response shapes:

```ts
type VaResponse<T> = {
  data: T;
  pagination?: { offset: number; limit: number; total: number };
  meta: { apiVersion: string };
};

export async function getVaResource<T>(resource: string, params: Record<string, string | number> = {}) {
  const url = new URL(`https://flyhub.app/api/v1/va/${resource}`);
  for (const [key, value] of Object.entries(params)) url.searchParams.set(key, String(value));

  const response = await fetch(url, {
    headers: { Authorization: `Bearer ${process.env.FHVA_API_KEY}` },
    cache: 'no-store',
  });
  const body = await response.json().catch(() => null) as VaResponse<T> | { error?: { code?: string; message?: string } } | null;
  if (!response.ok) {
    const error = body && 'error' in body ? body.error : null;
    throw new Error(`${response.status}: ${error?.code ?? 'request_failed'}`);
  }
  return body as VaResponse<T>;
}
```

For a list, continue while `offset + data.length < pagination.total`. Stop on a repeated response or an error; never retry a failed page indefinitely.

## Managing API keys from Ownership

These are signed-in airline-management routes used by the Ownership screen. They are included here so owners can understand the complete lifecycle, but they are not a replacement for the public read API.

Management requests use a normal FlyHub session bearer token:

```http
Authorization: Bearer <FlyHub-session-JWT>
```

The caller must have the `manage_ownership` capability for the airline. In the normal product flow, this is the current airline owner. A public website should never ask an airline owner to paste this session token into it.

Replace `{airlineId}` with the UUID shown in the airline's own management context.

### List integrations

```http
GET /api/va/airlines/{airlineId}/integrations
```

Returns the combined Ownership view:

```json
{
  "data": {
    "keys": [],
    "webhooks": []
  }
}
```

The response contains metadata only. It never returns an API key secret or the raw Discord webhook URL. The route is private and not cacheable.

### List API keys

```http
GET /api/va/airlines/{airlineId}/integrations/api-keys
```

Response:

```json
{
  "data": {
    "keys": [
      {
        "id": "<key-id>",
        "name": "Public airline website",
        "key_prefix": "fhva_live_XXXXXXXXXXX",
        "scopes": ["airline:read", "schedules:read"],
        "status": "active",
        "created_at": "2026-07-21T00:00:00.000Z",
        "expires_at": null,
        "last_used_at": "2026-07-21T12:00:00.000Z",
        "request_count": 42
      }
    ],
    "availableScopes": [
      "airline:read",
      "pilots:read",
      "flights:read",
      "fleet:read",
      "schedules:read",
      "statistics:read"
    ]
  }
}
```

Up to 50 key records are returned. The secret is never included in this list. Usage metadata is best effort, so `last_used_at` and `request_count` can briefly lag behind live traffic.

### Create an API key

```http
POST /api/va/airlines/{airlineId}/integrations/api-keys
Content-Type: application/json
```

Body:

```json
{
  "name": "Public airline website",
  "scopes": ["airline:read", "pilots:read", "schedules:read"],
  "expiresInDays": 365
}
```

Rules:

- `name` is trimmed and must be 2–80 characters;
- `scopes` must contain at least one valid scope; duplicate scope names are removed;
- `expiresInDays` must be `30`, `90`, `365`, or `null`;
- omitting `expiresInDays` is the same as `null`, meaning no expiry;
- an airline can have up to 10 active keys;
- extra JSON properties are rejected.

Successful creation returns `201`:

```json
{
  "data": {
    "key": {
      "id": "<key-id>",
      "name": "Public airline website",
      "key_prefix": "fhva_live_XXXXXXXXXXX",
      "scopes": ["airline:read", "pilots:read", "schedules:read"],
      "status": "active",
      "created_at": "2026-07-21T00:00:00.000Z",
      "expires_at": "2027-07-21T00:00:00.000Z",
      "last_used_at": null,
      "request_count": 0
    },
    "secret": "fhva_live_<public-part>_<secret-part>"
  }
}
```

Copy `secret` before leaving the response. It will not be recoverable later. The response is marked `private, no-store` so the one-time credential is not left in a shared cache.

### Revoke an API key

```http
DELETE /api/va/airlines/{airlineId}/integrations/api-keys/{keyId}
```

Successful response:

```json
{
  "data": {
    "revoked": true
  }
}
```

Revocation is a soft state change that preserves historical metadata and stops the key immediately. There is no restore endpoint. To rotate a key, create the replacement first, update your server, verify it, and then revoke the old key. This avoids an unnecessary outage.

## What the public API does not do

The current external API intentionally does not provide:

- flight booking, cancellation, dispatch, or completion;
- ticket-price changes or market/economy commands;
- pilot applications, role changes, suspensions, or removals;
- fleet leasing, transfers, positioning, configuration, or maintenance;
- route and schedule editing;
- FH$ transfers, settlement changes, or billing controls;
- ownership changes or airline retirement;
- generic custom webhooks, event replay, delivery history, or a webhook signature endpoint.

Use FlyHub's own screens for those actions. The next page, [Virtual Airlines Webhooks](./webhooks), documents the supported Discord notification integration.

## Integration checklist

Before giving an integration to your pilots:

- keep the API secret server-side;
- request only the scopes the page needs;
- handle `401`, `403`, `404`, `429`, and `503` explicitly;
- honor `Retry-After` and the reset headers;
- paginate instead of requesting large repeated pages;
- treat timestamps as UTC;
- ignore fields added in a later API version;
- do not assume a display name, flight number, or registration is a permanent UUID;
- rotate credentials by create → deploy → verify → revoke;
- remove the old key from logs, source control, CI output, and screenshots.
